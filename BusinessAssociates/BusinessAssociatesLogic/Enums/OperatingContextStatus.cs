namespace BusinessAssociatesLogic
{
    // JOEF:  We can probably consolidate some of the status stuff.
    public enum OperatingContextStatus
    {
        Pending = 1,
        Active = 2,
        Inactive = 3
    }
}